[package]
name = "worker"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = { version = "1" }
mktemp = "0.5.1"
proto = { path = "../../libs/proto" }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "process"] }
tonic = "0"
