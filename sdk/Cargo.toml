[package]
name = "sdk"
version = "0.1.0"
edition = "2024"

[dependencies]
log = "0"
env_logger = "0"
tonic = "0"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
proto = { path = "../proto" }
